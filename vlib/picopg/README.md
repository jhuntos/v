## Description:

`picopg` is a V implementation of [picoev](https://github.com/kazuho/picoev),
which in turn is "A tiny, lightning fast event loop for network applications".

picopg have implemented a dynamic routingtable and PostgreSQL connection

There are also an `picodb` for sqllite

MySQL and MSSQL will come soon enough...
